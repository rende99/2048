let keypress,Game;_afa‍.w("keypress",[["default",["keypress"],function(v){keypress=v}]]);_afa‍.w("./engine/game",[["default",["Game"],function(v){Game=v}]]);


keypress(process.stdin);


/**
 * The code in this file is used to run your game in the console. Use it
 * to help develop your game engine.
 *
 */

let game = new Game(2);

_afa‍.g.console.log(game.toString());


game.onMove(gameState => {
    _afa‍.g.console.log(game.toString());
    _afa‍.g.console.log(game.gameState);
});

game.onWin(gameState => {
    _afa‍.g.console.log('You won with a gameState of...', gameState)
});

game.onLose(gameState => {
    _afa‍.g.console.log('You lost! :(', gameState)
    console.log(`Your score was ${gameState.score}`);
});

process.stdin.on('keypress', function (ch, key) {
    switch (key.name) {
        case 'right':
            console.warn("\n\nmoving right:")
            game.move('right');
            break;
        case 'left':
            console.warn("\n\nmoving left:")

            game.move('left');
            break;
        case 'down':
            console.warn("\n\nmoving down:")
            game.move('down');
            break;
        case 'up':
            console.warn("\n\nmoving up:")
            game.move('up');
            break;
    }
    if (key && key.ctrl && key.name == 'c') {
        process.stdin.pause();
    }
});


process.stdin.setRawMode(true);
process.stdin.resume();

